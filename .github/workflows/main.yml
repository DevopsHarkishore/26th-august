name: "my first github action"

on:
 push:
   branches: 
     - 'feature/**'
     - 'main'
jobs:
 job:
   name: terraform init
   runs-on: mygithub-runner
   steps:
    - name: india
      run: echo i love my india

    - name: india
      run: echo welcome!

    - name: india
      run: echo most welcome
    

    - name: Install Terraform
      uses: little-core-labs/install-terraform@v2.0.0
      with:
       version: latest
          
      








      
